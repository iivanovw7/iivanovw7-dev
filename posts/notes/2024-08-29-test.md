---
image: "assets/jpg/note-cover.jpg"
title: Testing layout
date: 2024-08-29
description: It was popularised in the 1960s with the release of Letraset sheets containing Lorem Ipsum passages, and more recently with desktop publishing software like Aldus PageMaker including versions of Lorem Ipsum.
link: none
file_name: 2024-08-29-test
tags:
    - markdown
    - highlight
    - test
---

# This is a test to see how an `md` file is rendered

### Table of contents <a name="toc"></a>

-   <a href="#Headers">Headers</a>
-   <a href="#Text">Text</a>
-   <a href="#Links">Links</a>
-   <a href="#Code">Code</a>
-   <a href="#Images">Images</a>

<a name="Headers"></br></br></a>

## Headers

# Level 1 (h1)

## Level 2 Subheading (h2)

### Level 3 Subheading (h3)

#### Level 4 Subheading (h4)

##### Level 5 Subheading (h5)

###### Level 6 Subheading (h6)

<p align="right">(<a href="#toc">back to contents</a>)</p>

## Text <a name="Text"></a>

This is a paragraph of normal text. We can add more text to fill the space and see how it behaves This is a paragraph of normal text. We can add more text to fill the space and see how it behaves.

**Bold text** and _italic text_. We can also combine **text that is _bold and italic_**.

~~Strikethrough text~~.

> This is a blockquote. We can add multiple lines to the blockquote to test how the renderer handles it.

Inline code formatting: `like this`.

Some code with special characters: `<p>Hello & World;</p>`

Some `blockquote` formatting:

> This is a first-level blockquote.
> It has multiple lines.
>
> > This is a nested blockquote.
> > It can also have multiple lines.
>
> > -   And even contain lists!
> > -   Like this one.

Another `blockquote`:

> ### A heading inside a blockquote
>
> Some text within the blockquote after the heading.

<p align="right">(<a href="#toc">back to contents</a>)</p>

## Lists <a name="Lists"></a>

Unordered list

---

-   Item 1
-   Item 2
    -   Sub-item 2.1
    -   Sub-item 2.2
-   Item 3

Ordered list:

1. First item
2. Second item
    1. Sub-item 2.1
    2. Sub-item 2.2
3. Third item

Deeply nested unordered list:

-   Level 1
    -   Level 2
        -   Level 3

List with paragraphs:

-   First item
-   Second item
    Another paragraph here.

<p align="right">(<a href="#toc">back to contents</a>)</p>

## Links <a name="Links"></a>

Here's an inline link with a title: [Visit Example](https://www.example.com "This is the title").

An inline link to [Link Example](https://www.example.com).

<p align="right">(<a href="#toc">back to contents</a>)</p>

## Code <a name="Code"></a>

Some `javascript` code:

```javascript
async function highlight(code: string, language: string) {
  const themePath = require.resolve(
    '@pveyes/aperture/themes/Large-Aperture.json'
  );

  const theme = await loadTheme(themePath);
  const highlighter = await getHighlighter({ theme });
  const tokens = highlighter.codeToThemedTokens(code, language);

  return generateHTMLFromTokens(tokens);
}
```

Some `typescript` code:

```typescript
// Removes 'readonly' attributes from a type's properties
type CreateMutable<Type> = {
    -readonly [Property in keyof Type]: Type[Property];
};

type LockedAccount = {
    readonly id: string;
    readonly name: string;
};

type UnlockedAccount = CreateMutable<LockedAccount>;
```

Some `rust` code:

```rust
struct Person {
    name: String,
    age: u32,
}

impl Person {
    fn new(name: &str, age: u32) -> Self {
        person {
            name: string::from(name),
            age,
        }
    }

    fn say_hello(&self) {
        println!("{}, {}", self.name, self.age);
    }
}

```

<p align="right">(<a href="#toc">back to contents</a>)</p>

## Images <a name="Images"></a>

An inline image:
![Image Alternative Text](../assets/png/project-image.png)

Embedded `html` image:

<img alt="test image" width="300" src="../assets/png/project-image.png" />

<p align="right">(<a href="#toc">back to contents</a>)</p>
